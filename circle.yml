machine:
  python:
    version: 2.7.3
  services:
    - docker


dependencies:
  pre:
    - find ~/.cache/pip
    - pip install docker-compose
    - find ~/.cache/pip


test:
  post:
    - docker --version
    - docker-compose --version
