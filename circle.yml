machine:
  services:
    - docker
  pre:
    - sudo pip install wheel

dependencies:
  cache_directories:
    - ~/.cache/pip
  pre:
    - sudo -H pip install docker-compose
  post:
    - find ~/.cache/pip || true

test:
  post:
    - docker-compose version
