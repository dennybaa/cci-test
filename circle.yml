machine:
  services:
    - docker
  post:
    # - sed -i 's/.*pyenv.*//' ~/.circlerc
    - sudo -H pip install wheel docker-compose

dependencies:
  cache_directories:
    - /root/.cache/pip
  pre:
    - pwd
    - ls -la ./

test:
  post:
    - docker-compose version
