machine:
  python:
    version: 2.7
  services:
    - docker


dependencies:
  pre:
    - id
    - find ~/.cache/pip
    - sudo -H pip install docker-compose
    - find ~/.cache/pip


test:
  post:
    - docker-compose version
