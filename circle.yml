machine:
  services:
    - docker
  pre:
    - sudo -H pip install wheel

dependencies:
  cache_directories:
    - ~/.cache/pip
  pre:
    - sudo -H pip install docker-compose
  post:
    - find /root/.cache/pip || true

database:
  pre:
    - sudo mv /root/.cache/pip ~/.cache/pip
    - sudo find ~/.cache/pip || true

test:
  post:
    - docker-compose version
