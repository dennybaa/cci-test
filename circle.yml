machine:
  services:
    - docker


dependencies:
  cache_directories:
    - ~/.cache/pip
  pre:
    - id
    - pip --version
    - find ~/.cache/pip
    #- "sudo -H pip install --upgrade setuptools requests[security]"
    - pip install docker-compose
    - find ~/.cache/pip


test:
  post:
    - docker-compose version
